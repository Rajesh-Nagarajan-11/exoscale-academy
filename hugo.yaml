baseURL: https://layer5.github.io/exoscale-academy/
title: Exoscale Academy
canonifyURLs: true
ignoreLogs: ['warning-goldmark-raw-html']

module:
  hugoVersion:
    extended: true
    min: 0.146.0
  imports:

    # used to source the base theme for academy.
    - path: github.com/layer5io/academy-theme
      disable: false